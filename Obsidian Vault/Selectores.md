## 📌 Subtemas de Selectores CSS

1. **Básicos**
    - [[Selector de etiqueta]] (`p`, `h1`)
    - [[Selector de clase]] (`.clase`)
    - [[Selector de ID]] (`#id`)
2. **Combinados**
    - [[Descendiente]] (`.card p`)
    - [[Hijo directo]] (`.card > p`)
    - [[Adyacente]] (`h1 + p`)
3. **De atributo**
- `[type="text"]` [[tema 1]]
- `[href^="https"]` [[tema 2]]
1. **Pseudo-clases**
- `:hover`, `:focus`, `:active` [[tema 3]]
- `:first-child`, `:last-child`, `:nth-child()` [[tema 4]]
1. **Pseudo-elementos**
    - `::before` [[tema 5]]
    - `::after` [[tema 6]]
    - 